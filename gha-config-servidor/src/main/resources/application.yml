server:
  port: 7070

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/GROVER-JHA/numero1.git
          searchPaths: config-data
          default-label: main
  security:
    user:

      name: grover
      password: aprovar

